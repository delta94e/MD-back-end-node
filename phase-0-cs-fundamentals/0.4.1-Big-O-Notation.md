# §0.4.1 — Big-O Notation (Phân Tích Độ Phức Tạp)

> **Mục tiêu**: Hiểu Big-O, Big-Ω, Big-Θ từ gốc. Biết phân tích time & space complexity cho bất kỳ đoạn code nào. Amortized analysis. Đây là **kỹ năng nền tảng** — mọi câu phỏng vấn coding đều yêu cầu phân tích complexity.

---

## Mục Lục

1. [Big-O Là Gì — Ý Nghĩa Thực Sự](#1-big-o-là-gì--ý-nghĩa-thực-sự)
2. [Các Mức Complexity Phổ Biến](#2-các-mức-complexity-phổ-biến)
3. [Rules Phân Tích Big-O](#3-rules-phân-tích-big-o)
4. [Best / Worst / Average Case](#4-best--worst--average-case)
5. [Space Complexity](#5-space-complexity)
6. [Amortized Analysis](#6-amortized-analysis)
7. [Big-Ω và Big-Θ](#7-big-ω-và-big-θ)
8. [Phân Tích Code Thực Tế](#8-phân-tích-code-thực-tế)
9. [Câu Hỏi Phỏng Vấn](#9-câu-hỏi-phỏng-vấn)

---

## 1. Big-O Là Gì — Ý Nghĩa Thực Sự

Big-O mô tả **tốc độ tăng trưởng** của thời gian/bộ nhớ khi input tăng, **bỏ qua hằng số và bậc thấp**.

```
Ý tưởng: Khi n rất lớn, chỉ số hạng lớn nhất quan trọng.

f(n) = 3n² + 5n + 100
  Khi n = 10:    300 + 50 + 100 = 450
  Khi n = 1000:  3,000,000 + 5,000 + 100 = 3,005,100
  Khi n = 10^6:  3×10^12 + 5×10^6 + 100 ≈ 3×10^12

→ 5n và 100 trở nên VÔ NGHĨA so với 3n²
→ Hệ số 3 cũng bỏ (Big-O chỉ quan tâm TỐC ĐỘ TĂNG)
→ f(n) = O(n²)
```

**Big-O = upper bound** — "thuật toán KHÔNG BAO GIỜ chậm hơn mức này".

---

## 2. Các Mức Complexity Phổ Biến

```
Thời gian chạy khi n = 1,000,000 (1 triệu):

O(1)        →  1              ← Hash table lookup
O(log n)    →  20             ← Binary search
O(n)        →  1,000,000      ← Linear scan
O(n log n)  →  20,000,000     ← Merge sort
O(n²)       →  10^12          ← Bubble sort (1 TRIỆU GIÂY!)
O(2^n)      →  10^301029      ← Brute force combinations (VŨ TRỤ CHẾT!)
O(n!)       →  ∞              ← Traveling Salesman brute force

Biểu đồ tăng trưởng:
  │
  │                                        O(2^n)
  │                                      /
  │                               O(n²) /
  │                             /      /
  │                           /      /
  │                    O(n log n)  /
  │                   /        /
  │              O(n)        /
  │            /          /
  │       O(log n)     /
  │     ··────────···
  │  O(1)
  └──────────────────────────────── n
```

### Bảng tham chiếu nhanh

| Complexity | Tên          | Ví dụ                     | n = 10^6 khả thi? |
| ---------- | ------------ | ------------------------- | :---------------: |
| O(1)       | Constant     | Array access, hash lookup |        ✅         |
| O(log n)   | Logarithmic  | Binary search             |        ✅         |
| O(√n)      | Square root  | Prime check               |        ✅         |
| O(n)       | Linear       | Array scan                |        ✅         |
| O(n log n) | Linearithmic | Merge sort, heap sort     |        ✅         |
| O(n²)      | Quadratic    | Nested loops, bubble sort |     ⚠️ (chậm)     |
| O(n³)      | Cubic        | Floyd-Warshall            |   ❌ (n ≤ 500)    |
| O(2^n)     | Exponential  | Subsets, recursive fib    |    ❌ (n ≤ 25)    |
| O(n!)      | Factorial    | Permutations              |    ❌ (n ≤ 12)    |

> **Interview tip**: Khi interviewer nói "n up to 10^5" → solution phải ≤ O(n log n). "n up to 10^3" → O(n²) OK. "n up to 20" → O(2^n) OK.

---

## 3. Rules Phân Tích Big-O

### Rule 1: Bỏ hằng số

```javascript
// O(3n) = O(n)
for (let i = 0; i < n; i++) {} // n iterations
for (let i = 0; i < n; i++) {} // n iterations
for (let i = 0; i < n; i++) {} // n iterations
// Total: 3n → O(n)
```

### Rule 2: Bỏ bậc thấp hơn

```javascript
// O(n² + n) = O(n²)
for (let i = 0; i < n; i++) {
  // n
  for (let j = 0; j < n; j++) {} // × n = n²
}
for (let k = 0; k < n; k++) {} // + n
// Total: n² + n → O(n²)
```

### Rule 3: Nối tiếp → cộng. Lồng nhau → nhân.

```javascript
// Nối tiếp: O(A) + O(B)
for (let i = 0; i < A; i++) {} // O(A)
for (let j = 0; j < B; j++) {} // O(B)
// Total: O(A + B)

// Lồng nhau: O(A) × O(B)
for (let i = 0; i < A; i++) {
  // O(A)
  for (let j = 0; j < B; j++) {} //  × O(B)
}
// Total: O(A × B)
// ⚠️ CHỈ = O(n²) khi A = B = n!
```

### Rule 4: Log xuất hiện khi chia đôi

```javascript
// O(log n) — mỗi bước chia đôi search space
let i = n;
while (i > 1) {
  i = Math.floor(i / 2); // n → n/2 → n/4 → ... → 1
}
// Số bước: log₂(n)

// O(n log n) — n elements, mỗi element xử lý log n bước
// → Merge Sort: chia log n levels × merge n elements mỗi level
```

### Rule 5: Recursive — dùng recurrence relation

```javascript
// T(n) = T(n/2) + O(1) → O(log n)  (Binary Search)
// T(n) = 2T(n/2) + O(n) → O(n log n)  (Merge Sort)
// T(n) = T(n-1) + O(n) → O(n²)  (Selection Sort)
// T(n) = 2T(n-1) + O(1) → O(2^n)  (Naive Fibonacci)
```

---

## 4. Best / Worst / Average Case

```
Quick Sort example:
  Best case:    O(n log n)  — pivot luôn chia đều
  Average case: O(n log n)  — hầu hết trường hợp
  Worst case:   O(n²)       — pivot luôn min/max (sorted input!)

Phỏng vấn: luôn hỏi WORST CASE trừ khi nói rõ otherwise.

Ví dụ:
  Hash Table lookup:
    Best/Average: O(1)
    Worst: O(n) — tất cả keys cùng hash → linked list

  BST search:
    Best/Average: O(log n) — balanced
    Worst: O(n) — skewed (thành linked list)
```

---

## 5. Space Complexity

Đo lượng **bộ nhớ bổ sung** mà thuật toán sử dụng (không tính input).

```javascript
// O(1) space — in-place
function bubbleSort(arr) {
  for (let i = 0; i < arr.length; i++)
    for (let j = 0; j < arr.length - 1; j++)
      if (arr[j] > arr[j + 1]) [arr[j], arr[j + 1]] = [arr[j + 1], arr[j]];
  // Chỉ dùng thêm vài biến → O(1) extra space
}

// O(n) space — tạo array mới
function mergeSort(arr) {
  if (arr.length <= 1) return arr;
  const mid = Math.floor(arr.length / 2);
  const left = mergeSort(arr.slice(0, mid)); // tạo copy!
  const right = mergeSort(arr.slice(mid)); // tạo copy!
  return merge(left, right); // tạo result!
  // Extra space: O(n) cho arrays + O(log n) cho call stack
}

// O(n) space — recursion call stack
function fibonacci(n) {
  if (n <= 1) return n;
  return fibonacci(n - 1) + fibonacci(n - 2);
  // Call stack depth = n → O(n) space (dù time = O(2^n)!)
}
```

| Algorithm         | Time           | Space                    |
| ----------------- | -------------- | ------------------------ |
| Bubble Sort       | O(n²)          | **O(1)**                 |
| Merge Sort        | O(n log n)     | **O(n)**                 |
| Quick Sort        | O(n log n) avg | **O(log n)** call stack  |
| BFS               | O(V + E)       | **O(V)** queue           |
| DFS               | O(V + E)       | **O(V)** stack/recursion |
| Hash Map solution | O(n)           | **O(n)** map storage     |

---

## 6. Amortized Analysis

Một operation đôi khi đắt, nhưng **trung bình over n operations** thì rẻ.

```
Dynamic Array push:
  Push 1-4: O(1) mỗi lần (có chỗ trống)
  Push 5:   O(4) — resize! copy 4 elements + push 1
  Push 6-8: O(1) mỗi lần
  Push 9:   O(8) — resize! copy 8 elements + push 1
  ...

  n pushes: n + (1 + 2 + 4 + 8 + ... + n/2) copies ≈ n + n = 2n
  Amortized cost per push = 2n / n = O(1) ✓

  "Mỗi push TRẢ TRƯỚC 1 xu cho resize tương lai"
  → Khi resize xảy ra, đã tích đủ xu → chi phí = 0 amortized

Practical examples:
  Array.push()  → amortized O(1)
  Hash table insert → amortized O(1) (rehash đôi khi O(n))
  Splay tree    → amortized O(log n)
```

---

## 7. Big-Ω và Big-Θ

```
Big-O (O):  Upper bound  — "TỐI ĐA chậm bằng này"
            f(n) ≤ c × g(n) cho n đủ lớn

Big-Ω (Ω): Lower bound  — "TỐI THIỂU nhanh bằng này"
            f(n) ≥ c × g(n) cho n đủ lớn

Big-Θ (Θ): Tight bound  — "CHÍNH XÁC bằng này" (cả O và Ω)
            c₁ × g(n) ≤ f(n) ≤ c₂ × g(n) cho n đủ lớn

Ví dụ: Merge Sort
  O(n log n)  — worst case không vượt quá n log n ✓
  Ω(n log n)  — best case cũng ít nhất n log n ✓
  Θ(n log n)  — chính xác n log n (tight bound) ✓

Phỏng vấn: Hầu hết dùng Big-O. Nếu interviewer hỏi "tight bound?" → dùng Big-Θ.
```

---

## 8. Phân Tích Code Thực Tế

```javascript
// Ví dụ 1: O(n) hay O(n²)?
function example1(arr) {
  const set = new Set(arr); // O(n) — build set
  for (const item of arr) {
    // O(n) loop
    if (set.has(item * 2)) {
    } // O(1) lookup
  }
}
// O(n) + O(n × 1) = O(n) ✓

// Ví dụ 2: Tricky nested loop
function example2(n) {
  for (let i = 1; i < n; i *= 2) {
    // O(log n) — i doubles
    for (let j = 0; j < n; j++) {
      // O(n)
      // ...
    }
  }
}
// O(n log n) — log n outer iterations × n inner iterations

// Ví dụ 3: String concatenation trap!
function example3(n) {
  let s = "";
  for (let i = 0; i < n; i++) {
    s += "a"; // ⚠️ String immutable! Creates new string mỗi lần!
    //          Copy 1 + 2 + 3 + ... + n chars = O(n²)!
  }
}
// O(n²) — NOT O(n)! Fix: dùng Array.join() hoặc StringBuilder

// Ví dụ 4: Recursive analysis
function example4(n) {
  if (n <= 0) return;
  example4(n - 1);
  example4(n - 1);
}
// T(n) = 2T(n-1) + O(1)
// Mỗi level gấp đôi calls: 1 → 2 → 4 → 8 → ... → 2^n
// Total: O(2^n)
```

---

## 9. Câu Hỏi Phỏng Vấn

**Q: O(n) vs O(n log n) — khác biệt thực tế bao nhiêu?**

> A: Với n = 10^6: O(n) = 10^6 operations, O(n log n) = 2×10^7 (≈20x chậm hơn). Với n = 10^9: O(n) = 10^9, O(n log n) = 3×10^10 (≈30x). Trong thực tế, O(n log n) vẫn rất nhanh. Sự khác biệt thực sự quan trọng là O(n) vs O(n²): với n = 10^6, O(n²) = 10^12 — chênh 1 TRIỆU lần!

**Q: Amortized O(1) khác O(1) worst case thế nào?**

> A: O(1) worst case = LUÔN O(1), mọi operation. Amortized O(1) = trung bình O(1) over n operations, nhưng 1 operation CÓ THỂ là O(n) (ví dụ: array resize). Vấn đề: real-time systems (latency-sensitive) không chấp nhận amortized — 1 request chậm 100ms trong khi 99 requests nhanh 1μs vẫn là BUG.

**Q: Tại sao phỏng vấn hỏi time + space complexity?**

> A: Trade-off! Nhiều bài: giảm time complexity → tăng space (dùng hash map). Interviewer muốn biết bạn hiểu trade-off và chọn phù hợp context. Ví dụ: mobile app (ít RAM) → ưu tiên space. Backend batch job → ưu tiên time.

---

> **Checklist**:
>
> - [ ] Phân tích Big-O cho mọi đoạn code (nested loops, recursion)
> - [ ] Biết complexity của mọi data structure operations
> - [ ] Giải thích amortized analysis (dynamic array, hash table)
> - [ ] Time-Space trade-off: khi nào ưu tiên cái nào

---

## 10. Giải Thích Tường Tận — Từ Video

> 📺 Nguồn: [Algorithm Complexity](https://www.youtube.com/watch?v=cXCuXNwzdfY)

### 10.1 Tại Sao Đo Bằng GIÂY Không Được?

```
Ý tưởng đầu tiên: chạy algorithm → đo bao nhiêu giây → xong?

VẤN ĐỀ: Cùng 1 algorithm, chạy trên CPU KHÁC → kết quả KHÁC!

  CPU Intel i9:    0.3 giây
  CPU Apple M2:    0.2 giây
  CPU Raspberry:   5.0 giây

  → Kết quả phụ thuộc HARDWARE, không phải algorithm!
  → Ý tưởng đo bằng giây THẤT BẠI NGAY LẬP TỨC.

GIẢI PHÁP: Đếm SỐ BƯỚC (operations) thay vì giây.

  Quy ước đơn giản hóa:
    1 phép toán ngôn ngữ (a + b, i++, arr[i]) = 1 operation
    (Thực tế trên CPU: mỗi phép có thể = nhiều instructions,
     nhưng ĐƠN GIẢN HÓA để so sánh algorithms VỚI NHAU)
```

### 10.2 Đếm Operations — Từng Bước

```
VÍ DỤ 1: Constant — O(1)

  for (let i = 0; i < 100; i++) { ... }

  Luôn = 100 bước, DÙ input là gì.
  → Không phụ thuộc input → O(1) (constant)

VÍ DỤ 2: Linear — O(n)

  function process(n) {
    for (let i = 0; i < n; i++) { ... }
  }

  n = 10  → 10 bước
  n = 100 → 100 bước
  → Tỷ lệ thuận với n → O(n)

VÍ DỤ 3: Đếm CHÍNH XÁC rồi bỏ constants

  function example(n) {
    let a = 0;              // +1 operation
    for (let i = 0; i < n; i++) {
      // ...                // +n operations
    }
    a++;                    // +1 operation
  }

  Chính xác: 1 + n + 1 = n + 2 operations
  Bỏ constants: O(n + 2) = O(n) ✅

  TẠI SAO BỎ CONSTANTS?
    Vì quy ước "1 phép = 1 operation" đã là XẤPXỈ.
    Trên CPU khác nhau, a++ có thể = 1 hoặc 3 instructions.
    → Constants KHÔNG chính xác → bỏ luôn!
```

### 10.3 Ví Dụ O(√n) — Tưởng O(n) Nhưng Không Phải!

```
⚠️ KHÔNG PHẢI mọi vòng lặp đều O(n)!

function isPrime(n) {
  for (let i = 2; i * i <= n; i++) {
    if (n % i === 0) return false;
  }
  return true;
}

Thử với SỐCỤ THỂ:
  n = 16  → i chạy: 2, 3, 4 → 4 bước   (√16 = 4)
  n = 25  → i chạy: 2, 3, 4, 5 → 5 bước (√25 = 5)
  n = 121 → i chạy: 2, 3, ..., 11 → 11 bước (√121 = 11)

  Pattern: số bước = √n
  → O(√n) — KHÔNG PHẢI O(n)!

BÀI HỌC: Khi KHÔNG CHẮC complexity →
  thử với 3-4 số test → tìm pattern → xác định function.
```

### 10.4 Hai Vòng LặpKhác Biến — O(n + k)

```
function process(n, k) {
  for (let i = 0; i < n; i++) { ... }  // O(n)
  for (let j = 0; j < k; j++) { ... }  // O(k)
}

⚠️ KHÔNG PHẢI O(n)! Vì n ≠ k.
  → O(n + k)

NẾU 2 vòng lặp dùng CÙNG biến:
  for (let i = 0; i < n; i++) { ... }   // O(n)
  for (let j = 0; j < n; j++) { ... }   // O(n)
  → O(n + n) = O(2n) = O(n)              (bỏ constant 2)

QUY TẮC:
  Cùng biến → cộng rồi bỏ constant
  Khác biến → GIỮ CẢ HAI: O(n + k)
```

### 10.5 O(2^n) — Vẽ Call Tree Để Thấy

```
function f(n) {
  if (n <= 0) return;
  f(n - 1);    // gọi lại 2 lần
  f(n - 1);
}

Vẽ CALL TREE:

  n = 3:                    f(3)
                           /    \
                        f(2)    f(2)
                       /   \   /   \
                    f(1) f(1) f(1) f(1)
                    /\   /\   /\   /\
                   ..  ..  ..  ..  ..

  n = 3 → 7 calls   = 2³ - 1 = 7
  n = 4 → 15 calls  = 2⁴ - 1 = 15
  n = 5 → 31 calls  = 2⁵ - 1 = 31

  Pattern: 2^n - 1 calls → O(2^n)

  Mỗi call = O(1) → tổng = O(1) × O(2^n) = O(2^n)

NOTE: Fibonacci tree có ÍT nodes hơn (vì f(n-1) và f(n-2)):
  Complexity thực = O(1.618^n) ≈ O(φ^n)
  Nhưng thường LÀMTRÒN thành O(2^n) cho đơn giản.
```

### 10.6 O(n!) — Permutations

```
function permute(arr, l = 0) {
  if (l === arr.length - 1) { print(arr); return; }
  for (let i = l; i < arr.length; i++) {
    swap(arr, l, i);
    permute(arr, l + 1);
    swap(arr, l, i);
  }
}

Call tree cho n = 3, 4, 5:
  n = 3 → 16 nodes
  n = 4 → 65 nodes
  n = 5 → 326 nodes

Phân tích:
  Level cuối = n! leaves (3! = 6, 4! = 24, 5! = 120)
  Chiều cao tree = n + 1
  Mỗi node chạy loop ≈ n iterations

  → Complexity ≈ O(n × n!)

  LÀM TRÒN thô: O(n!)
  Chính xác hơn: O(n × n!)

  ⚠️ Với n = 12: 12! = 479,001,600 → ~500 TRIỆU operations
  ⚠️ Với n = 15: 15! = 1.3 × 10^12 → KHÔNG KHẢ THI!
```

### 10.7 Space Complexity — Quy Tắc Iterative vs Recursive

```
╔═════════════════════════════════════════════════════════════════╗
║  QUY TẮC NHANH:                                                ║
╠═════════════════════════════════════════════════════════════════╣
║                                                                 ║
║  ITERATIVE (vòng lặp):                                          ║
║    → Space = O(1) (thường)                                      ║
║    Vì: mỗi iteration chỉ dùng 1 vài biến,                      ║
║         KHÔNG tạo stack frame mới.                               ║
║                                                                 ║
║  RECURSIVE:                                                      ║
║    → Space = O(depth) = thường O(n)                              ║
║    Vì: mỗi recursive call TẠO 1 stack frame.                    ║
║         Stack frames CHỒNG CHẤT cho đến base case.              ║
║                                                                 ║
╚═════════════════════════════════════════════════════════════════╝

VÍ DỤ 1 — Iterative loop gọi function:

  for (let i = 0; i < n; i++) {
    doSomething(i);     // tạo stack frame
  }                     // hết → stack frame bị POP
                        // luôn chỉ có 1 frame trên stack
  → Space: O(1)

VÍ DỤ 2 — Factorial recursive:

  function factorial(n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);
  }

  Stack khi n = 5:
    ┌───────────────┐
    │ factorial(1)  │ ← đang chạy
    │ factorial(2)  │
    │ factorial(3)  │
    │ factorial(4)  │
    │ factorial(5)  │ ← gọi đầu tiên
    └───────────────┘

  → n frames trên stack → Space: O(n)

VÍ DỤ 3 — 2 recursive calls (f(n-1), f(n-1)):

  Dù call tree có 2^n nodes, stack KHÔNG chứa tất cả!
  Stack chỉ giữ 1 NHÁNH dài nhất tại mỗi thời điểm.
  Nhánh dài nhất = n → Space: O(n) (KHÔNG PHẢI O(2^n)!)
```

### 10.8 String Concatenation — Arithmetic Progression

```
let s = "";
for (let i = 0; i < n; i++) {
  s += "a";    // ← tạo string MỚI mỗi lần!
}

Strings là IMMUTABLE trong hầu hết languages:
  s += "a" = tạo string mới = copy toàn bộ s cũ + thêm "a"

  Iteration 1: copy 0 chars + add 1 → 1 operation
  Iteration 2: copy 1 char  + add 1 → 2 operations
  Iteration 3: copy 2 chars + add 1 → 3 operations
  ...
  Iteration n: copy n-1 chars + add 1 → n operations

  Tổng = 1 + 2 + 3 + ... + n = ARITHMETIC PROGRESSION

  Công thức: n(n+1)/2 = n²/2 + n/2

  Kiểm tra: n = 10:
    1 + 2 + 3 + ... + 10 = 55 = 10(11)/2 = 55 ✅

  Bỏ constants: n²/2 + n/2 → O(n²)

  ⚠️ s += "a" trong loop = O(n²), KHÔNG PHẢI O(n)!

  FIX:
    const parts = [];
    for (let i = 0; i < n; i++) parts.push("a");  // O(1) amortized
    const s = parts.join("");                       // O(n)
    // Tổng: O(n) — tốt hơn RẤT NHIỀU!
```

---

> **Checklist bổ sung**:
>
> - [ ] Biết tại sao đo bằng giây không chính xác
> - [ ] Phân biệt O(n) vs O(√n) bằng cách thử test numbers
> - [ ] Vẽ call tree cho recursive → đếm nodes → xác định complexity
> - [ ] Space complexity: iterative = O(1), recursive = O(depth)
> - [ ] String concatenation trong loop = O(n²), dùng Array.join() = O(n)
